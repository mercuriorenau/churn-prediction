# Churn prediction
<img width="577" height="392" alt="Screenshot 2026-07-14 at 8 01 15 p m" src="https://github.com/user-attachments/assets/4039761f-51a0-422a-9860-593057ee29e1" />

Predict which customers are most likely to leave (churn), with cohort analysis and explainability (SHAP).

---

## Proof it works

The project has been run end-to-end. Here are the results:

**Model comparison (test set)**  
| Model | Accuracy | F1 | Recall (churn) | Precision (churn) | AUC | Threshold |
|-------|----------|-----|---------------|-------------------|-----|-----------|
| Baseline (dummy) | 0.50 | 0.39 | 0.50 | | | |
| Logistic Regression | 0.78 | 0.57 | 0.62 | | | |
| **XGBoost** | **0.75** | **0.62** | **0.77** | **0.52** | **0.84** | **0.53** |

XGBoost gives the best recall on churners (who we care most about). Full table: [reports/model_comparison.csv](reports/model_comparison.csv).

**Sample predictions**  
Each customer gets a churn prediction (0/1) and a probability. Example: [reports/predictions_sample.csv](reports/predictions_sample.csv).

**Visuals** (generated by `python src/train.py`)  
- [reports/confusion_matrix.png](reports/confusion_matrix.png) – Confusion matrix  
- [reports/roc_curve.png](reports/roc_curve.png) – ROC curve with AUC  
- [reports/metrics_summary.png](reports/metrics_summary.png) – Bar chart: Accuracy, F1, Recall (churn), Precision (churn)
- [reports/shap_summary.png](reports/shap_summary.png) – SHAP feature impact on churn

**Local API latency** (FastAPI `POST /predict` via TestClient; see [reports/api_latency.txt](reports/api_latency.txt)): regenerate with `python scripts/bench_api.py` after training.

**Pipeline**  
1. Data in `data/` → 2. `python src/train.py` → 3. `python src/predict.py` → 4. Predictions in `reports/`.

---

## What this project shows

- **EDA** – Churn distribution, correlations, tenure and contract behavior, cohort retention.
- **Feature engineering** – Categorical encoding (one-hot), numeric cleaning; tree-based model so no scaling.
- **Model comparison** – Baseline (dummy), Logistic Regression, XGBoost; metrics table and visuals.
- **Interpretability** – SHAP (which variables drive churn); summary plot from `train.py` and the modeling notebook.

---

## Business insights (Telco dataset)

- Month-to-month contracts are associated with much higher churn than one- or two-year contracts.
- Lower tenure (newer customers) is associated with higher churn risk.
- Higher monthly charges, when combined with month-to-month contracts, tend to increase churn.

*(Run the EDA notebook to reproduce and extend these.)*

---

## Cómo lo corro (resumen)

**Sí tienes que instalar** las librerías una vez. Luego solo necesitas el CSV de datos.

1. **Abre terminal** en la carpeta del proyecto: `churn-prediction` (donde está este README).

2. **Instala dependencias (solo la primera vez):**
   ```bash
   pip install -r requirements.txt
   ```

3. **Descarga el dataset** de Kaggle [Telco Customer Churn](https://www.kaggle.com/datasets/blastchar/telco-customer-churn) y guarda el CSV dentro de la carpeta `data/`. Si el archivo se llama distinto, renómbralo a `WA_Fn-UseC_-Telco-Customer-Churn.csv` o cambia el nombre en el código.

4. **Entrenar el modelo:**
   ```bash
   python src/train.py
   ```
   Se crean los archivos en `models/` y gráficos en `reports/` (incluye SHAP).

5. **Hacer predicciones:**
   ```bash
   python src/predict.py
   ```
   Se genera `reports/predictions.csv`.

**Opcional**
- Notebooks: abre y ejecuta en orden `01_eda_churn.ipynb` → `02_modelado_churn.ipynb`.
- **Dashboard:** `streamlit run src/dashboard.py` (filtros por contrato, vista de datos).
- **API:** `uvicorn src.api:app --reload` (desde la carpeta del proyecto). Luego `POST http://localhost:8000/predict` con body `{"features": {"col1": val1, "col2": val2, ...}}` usando los nombres de columnas que guardó el entrenamiento (ver `models/columnas.pkl` o una fila de `reports/predictions_sample.csv` + preparar_datos para ver la lista).
- **Tests:** `pytest`
- **API latency:** `python scripts/bench_api.py` (writes `reports/api_latency.txt`)

---

## What’s in the repo

- **Notebooks:** EDA (`01_eda_churn.ipynb`) and modeling with baseline comparison (`02_modelado_churn.ipynb`).
- **Scripts:** `src/train.py` (train and save model), `src/predict.py` (run predictions), `src/dashboard.py` (Streamlit), `scripts/bench_api.py` (API latency).
- **Tests:** `tests/` (data prep + FastAPI `/predict`).
- **Data:** You add the CSV (not in repo). **Models** are generated when you train (not in repo). Committed **reports** show metrics and plots.

## Dataset

Download **Telco Customer Churn** from Kaggle and save the CSV in `data/`:

- [Kaggle – Telco Customer Churn](https://www.kaggle.com/datasets/blastchar/telco-customer-churn)

Use the default filename `WA_Fn-UseC_-Telco-Customer-Churn.csv` or change it in the code.

## How to run (show it works)

From the project root (`churn-prediction/`):

```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Put the Telco CSV in data/

# 3. Train (saves model to models/, plots to reports/)
python src/train.py

# 4. Predict (writes reports/predictions.csv)
python src/predict.py

# 5. Tests
pytest

# 6. Optional: API latency note
python scripts/bench_api.py
```

Optional:

- Run the notebooks in order for full EDA and model comparison (baseline, logistic regression, XGBoost).
- **Dashboard:** `streamlit run src/dashboard.py`  
  - Overview: churn rate and churn by contract type (bar charts).  
  - Segment explorer: filters by contract, tenure and monthly charges.  
  - Quick scoring: small form to see the model’s churn prediction for a synthetic customer.
- **API:** `uvicorn src.api:app --reload`

## Docker

Train locally first so `data/` (CSV) and `models/` (`.pkl` files) exist, then:

```bash
docker compose up --build
```

- API: http://localhost:8000  
- Dashboard: http://localhost:8501  

## Results you’ll get

- **reports/confusion_matrix.png** – Confusion matrix (from `train.py`).
- **reports/roc_curve.png** – ROC curve and AUC (from `train.py`).
- **reports/metrics_summary.png** – Bar chart of Accuracy, F1, Recall, Precision (from `train.py`).
- **reports/shap_summary.png** – SHAP feature impact (from `train.py`).
- **reports/model_comparison.csv** – Comparison of baseline, logistic regression, XGBoost (notebook + `train.py`).
- **reports/api_latency.txt** – Local `POST /predict` mean and p95 latency (from `scripts/bench_api.py`).
- **reports/predictions.csv** – Churn prediction and probability per customer (from `predict.py`).

After running once, you can commit report artifacts so the repo clearly shows the project works.

## Project structure

```
├── data/              # Put Telco CSV here (not in repo)
├── models/             # Saved model after train.py (not in repo)
├── notebooks/          # EDA + modeling
├── reports/            # metrics, plots, sample predictions, latency note
├── scripts/
│   └── bench_api.py
├── src/
│   ├── preparar_datos.py
│   ├── train.py
│   ├── predict.py
│   ├── evaluate.py
│   ├── dashboard.py
│   └── api.py          # FastAPI: POST /predict
├── tests/
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── LICENSE
└── README.md
```
